[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm rebuild better-sqlite3"]

[start]
cmd = "node server.js"
